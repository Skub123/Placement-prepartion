Write a program to find number in an array where all numbers except one are present twice.

#include <iostream>
using namespace std;
int uniqueValue(int arr[], int n){
    int xorval =0;
    for(int i =0; i<n;i++){
        xorval = xorval^arr[i];
        
    }
    return xorval;
}

int main() {
	// your code goes here
	int arr[] = {1,4,5,9,10,1,5,4,9};
	cout<<uniqueValue(arr, 9)<<endl;
	
	return 0;
}

